# Tomcat
server:
    tomcat:
        uri-encoding: UTF-8
        max-threads: 1000
        min-spare-threads: 30
    port: 8081
    context-path: /
spring:
    profiles:
        active: dev

storage:
    webPath: http://localhost
    imagePath: http://localhost
